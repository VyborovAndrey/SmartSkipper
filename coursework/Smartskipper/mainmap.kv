#:include trackloader.kv

BoxLayout:
    orientation: "vertical"
    MDTopAppBar:
        id: topbar
        left_action_items: [["play", lambda x: mapview.on_play()], ["file", lambda x: app.load_dialog.show_load()]]
    MapViewWidget:
        id: mapview